---
description: 初始化一个根目录预设空间的 PANGEA 项目（调试快捷入口）
agent: dispatcher
---

用户参数：`$ARGUMENTS`

加载 `project-workspace` Skill，调用：
`python -m tooling.pangea_cli project init --project-id <id> [--source-root source/<id>]`

正常自然语言场景中应自动完成，不要求用户记住本命令。
