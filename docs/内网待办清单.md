# PANGEA-TEST 内网待办清单

> 用途：汇总需回内网 codeagent 环境处理的事项，供逐项验证。
> 三类：**M-**（迁移类，需 Codetalks/内部资产原文件）· **T-**（验证类，需 codeagent 实测）· **A-**（决策类，需用户审定）。
> 权威出处：[architecture.md 附录](architecture.md#附-内网待办清单)、[requirements.md](requirements.md)。本文记录的新裁定优先于旧架构描述，架构文档后续同步收敛。

---

## ⭐ 开工第一步（阻断性）

- [ ] **T-1 先行**：在仓库根目录直接启动 `opencode`，调用项目自检 Agent **`@ping`**（`.opencode/agents/ping.md`）：
  - 验证根目录 `.opencode/agents/`、`.opencode/commands/`、`.opencode/skills/` 是否被当前 codeagent/OpenCode 版本发现；
  - 验证 Tab 可切换 `dispatcher`、`dev-expert`、`troubleshooter`、`test-designer`；
  - 验证隐藏能力 Agent 不出现在普通菜单，但授权族 Agent 可通过 Task 调用；
  - 验证 Skill `module-full-analysis` 的发现与加载；
  - 验证 `core/` 仓根相对路径、中文路径、Read/Glob/Grep；
  - 验证 `edit/bash deny` 是否连带阻断只读检索；若 codeagent 把 grep 走 shell，按实际权限语义仅放行只读检索；
  - 入内网使用 `git clone`/`git bundle`，勿走 zip；建议 `git config core.quotepath false`。

> Windows 不再需要 `ln -s`、Junction 或 `robocopy`。直接在仓库根目录执行 `opencode`。

---

## M 类 —— 迁移（需 Codetalks / 内部资产原文件）

> 纪律：**逐字继承改造，不得凭简介重写**。现有骨架中的 `<!-- 待迁移 -->` 占位需回内网用原文补齐。

- [ ] **M-1** `core/shared/八问纲领.md` —— 迁移 Codetalks 八问分析纲领全文。
- [ ] **M-2** `core/scenarios/模块全量分析.md` —— 逐字继承 Codetalks 九步全链路分析话术。
- [ ] **M-3** `core/scenarios/MR问题单分析.md` —— 迁移九步链路裁剪版。
- [ ] **M-4** `core/scenarios/日志定位.md` —— 迁移“问题根因辅助定位”研判流程。
- [ ] **M-5** `core/templates/黑盒用例.md` —— 用团队版替换通用占位版。
- [ ] **M-6** mr-reader 内部实现 —— 迁移团队已有 `mr_reader` skill，保持 `mr_summary` 契约。
- [ ] **M-7a** `core/scenarios/专项风险分析.md` —— 迁移专项风险分析流程。
- [ ] **M-7b** 透镜库“典型历史缺陷”栏 —— 团队回填历史缺陷案例。
- [ ] **M-8** Coverage Gate —— 对照 Codetalks 原版核对覆盖审计机制。
- [ ] **M-9** auditor 提示词 —— 迁移独立 Judge 原版提示词。

---

## T 类 —— 验证/确认（需 codeagent 实际环境）

- [ ] **T-1** 根目录项目配置、Tab、隐藏 Agent、Skill、中文路径及权限语义实测。（见上方）
- [x] **T-2** scenarios 落地路线已裁定：`core/` 保持唯一平台无关事实来源，`.opencode/skills/` 做薄包装，不复制方法论正文。
- [ ] **T-3** 并发派 Subagent 的上限与返回体量限制，确定深度模式批大小。
- [ ] **T-4** MCP CodeHub 类工具是否存在及名称，验证泛化探测。
- [ ] **T-5** Windows 上 `runtime/runctl.py` 的时间、路径和原子替换行为；任务 ID 日期由 Python 本机时间生成。
- [ ] **T-6** 被 Task 调用的族 Agent 能否与用户多轮对话；复核询问前置与“待用户确认”设计。
- [ ] **T-7** 零依赖标准库校验器与可选 `jsonschema` 严格校验结果是否一致。

---

## A 类 —— 用户审定（架构决策）

- [ ] **A-1** 挖掘剧本清单是否增删。
- [ ] **A-2** 交接工件 Schema 字段是否认可。
- [ ] **A-3** Dispatcher 模式判定、能力菜单和场景衔接是否认可。
- [x] **A-4** 三个族 Agent 采用 `mode: all`：支持 Tab 直接使用，也支持 Dispatcher Task 调用。内部能力 Agent 保持 `subagent + hidden`。
- [ ] **A-5** P10 初始化与卸载时序、P11 配置规格盘点是否采纳。

---

## 已裁定

### 2026-07-25 Windows 零部署改造

- OpenCode 配置提升到仓库根目录 `.opencode/`，不再使用部署映射。
- 族 Agent 为 `mode: all`；能力 Agent 为 `subagent + hidden`。
- `runtime/runctl.py` 默认仅依赖 Python 标准库；`jsonschema` 是可选严格校验增强。
- Auditor 壳提前至 M1；M1 附带种子两库与风险扫描剧本。

## 里程碑状态

- **平台无关资产**：M1/M2/M3 骨架已落地。
- **机器运行时**：`module-full-analysis` 已接入 Registry、Schema、Run Store，待内网真实环境验收。
- **生产可用性**：仍取决于 M 类内容迁移和 T 类实测，不能仅以文件数量判定完成。
