# PANGEA v1 repository guidance

产品流程、契约、Review 和完成条件只以 `PANGEA.md` 为准。实现不得创建第二套生产路径、
兼容 fallback、隐藏 schema、SHA 完整性门禁或重复规范文档。

开发时保留用户已有改动和 `pangea-data/` 历史 Run。`.codebuddy/` 属于用户文件，不读取、
不修改、不删除、不纳入提交。
